import './App.css';

// to run the json server run => json-server --watch db.json

function App() {
  return (
    <div className="App">
      <h1>IPA APP</h1>
      <h2>Who needs API's when you have IPA's</h2>
    </div>
  );
}

export default App;
